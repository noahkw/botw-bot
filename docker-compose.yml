version: "2.1"
services:
  botw-bot:
    image: docker.pkg.github.com/noahkw/botw-bot/botw-bot:latest
    volumes:
      - {CLONE_DIR}:/app
    restart: always
    environment:
      - PYTHONUNBUFFERED=1
  db:
    image: postgres
    volumes:
      - {DATA_DIR}:/var/lib/postgresql/data
    restart: always
    environment:
      POSTGRES_PASSWORD: XXXXXXXXX
      POSTGRES_USER: botw-bot
